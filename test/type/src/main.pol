
type Check(a) {
   Ctor1(val: a)
   Ctor2
}


pub fn check_inference(arg) {
    let b = Ok(12)
    let c = Err(arg) == b
    b
}

pub fn main() {
    let b = "test"
    let a: nocrypt String = b
    check_inference(a)
    check_inference("test")
}
