
type Check(a) {
   Ctor1(val: a)
   Ctor2
}

type Other {
   Check(a: Check(I32))
}


pub fn check_inference(arg) {
    let b = Ok(12)
    let c = Err(arg) == b
    b
}

pub fn main() {
    let b: nocrypt String = "test"
    let a: nocrypt String = b
    a |> check_inference

    let c = Ctor1(12)
    c
}
